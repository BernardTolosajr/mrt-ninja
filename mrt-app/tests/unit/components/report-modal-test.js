import {
  moduleForComponent,
  test
} from 'ember-qunit';

moduleForComponent('report-modal', 'ReportModalComponent', {
  // specify the other units that are required for this test
  // needs: ['component:foo', 'helper:bar']
});
